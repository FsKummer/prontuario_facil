<%= turbo_frame_tag 'content' do %>
  <div class="p-6 bg-white rounded-lg shadow-lg">
    <%= render "shared/alerts_notices" %>
    <h3 class="text-2xl font-bold text-gray-800 mb-4">Prontuários</h3>
    <ul class="space-y-3">
      <% @prontuarios.each do |prontuario| %>
        <li><a href="#" class="block p-4 bg-cyan-50 rounded-lg shadow-sm hover:bg-cyan-100"><%= prontuario.nome %></a></li>
      <% end %>
    </ul>
    <div class="mt-4">
      <%= link_to 'Criar', new_prontuario_path, data: { turbo_frame: 'content' }, class: "inline-block px-6 py-2 text-lg font-medium text-white bg-cyan-600 rounded-lg hover:bg-cyan-500" %>
    </div>
  </div>
<% end %>
